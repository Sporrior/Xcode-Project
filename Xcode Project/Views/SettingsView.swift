import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            // Settings options will be added here.
            Text("Settings")
        }
        .navigationTitle("Settings")
    }
}
